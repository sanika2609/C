
#include <stdio.h>

int main() {
       float l,b,area;
       printf("Enter the length of rectangle");
       scanf("%f",&l);
       printf("Enter the breadth");
       scanf("%f",&b);
       area=l*b;
       printf("The area of rectangle is:%f",area);

    return 0;
} 
