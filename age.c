#include<stdio.h>
int main()
{
    int age;
    if(age>=18)
    {
               printf("The person can vote);
               }
               else
               {
                   printf("The person can't vote);
                   }
                   return 0:
                          }
