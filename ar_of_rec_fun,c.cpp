#include <stdio.h>

void area() {
    int length;
   int breadth;
}
int main(){
    int area,length=5,breadth=6;
    
    int r=(length*breadth);
    printf("Area of the rectangle is:%d",r);

    
       return 0;
}
